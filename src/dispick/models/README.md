# Packaged model

`Picker.load()` looks here for `dispick.onnx` and its card `dispick.json` when neither a path
nor `$DISPICK_MODEL` is given. Put a trained model's two files here to ship it with the
package (see docs/cloud.md for where they come from).
